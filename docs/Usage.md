---
title: 使用教程
---

<section class="dp-page-hero">
  <div class="dp-eyebrow">User Guide</div>
  <h1>导航页与后端容器完整使用说明</h1>
  <p>本页按产品实际功能分区整理：第一类是导航页功能，第二类是后端容器与 Agent 节点管理，第三类是底部与系统配置。</p>
</section>

## 总体工作方式

<div class="dp-flow">
  <div><strong>读取 Docker</strong><span>读取容器、镜像、端口、网络、状态和 Compose 信息。</span></div>
  <div><strong>自动新建编辑</strong><span>自动生成卡片，补全名称、分组、图标、地址和运行状态。</span></div>
  <div><strong>配置导航页</strong><span>调整卡片、背景、页面、搜索、分组和内外网访问模式。</span></div>
  <div><strong>管理节点</strong><span>在同一面板管理本机与 Agent 节点的容器、Compose、镜像、更新和定时重启。</span></div>
</div>

<div class="dp-usecase-grid">
  <div class="dp-panel"><h3>导航页功能</h3><p>面向前台展示，负责卡片、背景、页面、自动地址、隐藏容器、搜索、导航设置和分组管理。</p></div>
  <div class="dp-panel"><h3>后端与 Agent 管理</h3><p>面向 Docker 管理，负责本机与远程节点、容器、Compose、定时重启、镜像、图标库、代理和 Docker 设置。</p></div>
  <div class="dp-panel"><h3>系统操作</h3><p>面向日常使用，负责明暗模式、系统设置、退出登录、备份与维护。</p></div>
</div>

![导航页 / 自动生成卡片后的首页](/assets/wiki/usage-navigation-overview.png)

## 前端导航页

前端导航页对应设置面板中的“外观”和“功能”区域，用于控制服务展示、容器卡片、自动地址、分组、搜索和集成服务。

### 卡片设置

卡片设置控制导航卡片的展示风格和信息密度。

可配置项：

- 卡片背景透明度。
- 卡片圆角。
- 图标形状。
- 容器状态提示。
- CPU / 内存显示。
- 新增卡片默认位置。

开启容器状态提示后，导航卡片会显示正常运行、异常或错误、已停止等状态；开启 CPU / 内存显示后，可直接在导航页观察容器资源占用。

![卡片设置 / 透明度与状态提示](/assets/wiki/card-settings.png)

### 背景设置

背景设置用于调整导航页整体视觉。

可配置项：

- 上传背景图。
- 可通过 URL 地址下载背景。
- 背景模糊。

建议背景不要过亮或过花，否则会影响卡片和文字识别。

### 页面设置

页面设置用于控制导航页整体布局、Dashboard 显示内容、滚动方式和页脚信息，是决定导航页使用体验的核心配置之一。

页面样式可调整：

- **页面最大宽度**：控制导航内容区域的最大展示宽度，适合不同分辨率屏幕和大屏展示。
- **分组卡片底边距**：调整分组之间的纵向间距，让不同服务分组更容易区分。
- **系统状态大小**：控制顶部系统状态信息的展示比例。
- **抽屉栏透明度**：调整设置面板或抽屉区域的透明效果，让背景和面板层次更协调。

Dashboard 显示配置可控制首页顶部内容：

- 显示标语。
- 显示时间。
- 显示搜索栏。
- 显示系统状态，点击标语或时间可切换系统状态展示。

滚动模式可选择：

- **整个页面滚动**：页面整体一起滚动，适合服务卡片较多、希望浏览完整页面的场景。
- **分组卡片滚动**：分组区域独立滚动，适合希望顶部信息保持稳定、只滚动服务卡片列表的场景。

页脚可填写备案信息、版权说明或其他自定义文字。不填写时导航页不会显示页脚；填写后可选择显示位置，例如底部居中。

![页面设置 / 页面样式与 Dashboard 显示配置](/assets/wiki/page-settings.png)

### 标语时间

标语时间用于首页顶部展示个性化文案、时间和日期。

可以用于：

- 显示一句固定标语。
- 显示当前时间。
- 显示日期与星期。
- 让导航页更像个人 NAS 首页。

如果截图准备公开发布，建议隐藏过于个人化的标语。

### 全局设置

全局设置用于控制导航页通用行为，例如显示策略、默认交互、同步状态和基础偏好。

常见用途：

- 保存全局显示偏好。
- 控制默认卡片行为。
- 管理设置同步状态。
- 调整面板基础交互。

### 自动地址

自动地址用于批量补全导航卡片的内网地址、外网地址和图标。填写基础地址后，Docker-Panel 会读取 Docker 容器的宿主机映射端口，根据规则自动生成可访问链接，并把结果写入未编辑过的容器卡片。

#### 地址规则

| 配置项 | 说明 |
| --- | --- |
| 内网地址 | 填写 Docker 主机的内网 IP 或主机名，例如 `192.168.1.10`。不需要填写 `http://`、端口和路径。 |
| 外网地址 | 可填写公网 IP 或域名。公网 IP 会使用 `http://` 并自动追加容器端口；域名会使用 `https://`，且不会追加端口。 |

#### 自动补全规则

- **IP 地址**：固定使用 `http://`，并自动追加读取到的宿主机端口。
- **域名地址**：固定使用 `https://`，只使用填写的域名，不自动追加端口。
- **图标获取**：优先使用内网地址获取图标；内网不可用时使用外网地址；仍未匹配时使用图标库或默认图标。
- **未编辑卡片**：普通应用会自动跳过已经手动编辑过的卡片，避免覆盖你的自定义配置。
- **强制更新**：需要一次确认，会覆盖卡片地址和图标，适合批量重新整理所有入口。

#### 操作方式

1. 填写 Docker 主机的内网地址。
2. 如果需要远程访问，再填写外网地址。
3. 点击“应用到未编辑卡片”，系统会为未手动编辑过的卡片补全地址和图标。
4. 第一次安装使用，先点击“强制更新所有卡片”，确认后会覆盖已有地址和图标；后续新入容器卡片会自动写入地址和图标。

![自动地址 / 地址规则与批量补全](/assets/wiki/automatic-address.png)

#### 按节点预设地址

自动地址支持选择本机或任意 Agent 节点分别配置。每个节点都可以预先填写自己的内网地址与外网地址；Agent 在线时也可使用自动检测，检测结果不符合实际网络时可改为手动设置。

节点容器被识别后，会按所属节点的地址预设拼接宿主机映射端口，再自动写入对应导航卡片，不会混用其他服务器的访问地址。

![自动地址 / Agent 节点地址预设](/assets/wiki/agent-node-addresses.png)

### 容器卡片管理

当 Docker 中出现新容器时，面板会根据容器信息创建导航卡片。系统会尽量推断：

| 信息 | 来源 |
| --- | --- |
| 服务名称 | 容器名、镜像名、Docker Labels。 |
| 分组 | 默认分组、Docker Labels、后台分组规则。 |
| 图标 | 图标库、镜像名、服务名、URL 获取、手动上传。 |
| 内网地址 | 宿主机地址和端口映射。 |
| 运行状态 | Docker 容器状态和健康状态。 |

这套能力的目的就是减少手动维护：不用每次新建容器后再找端口、复制地址、创建卡片、找图标和放分组。

### 隐藏容器

不是所有容器都适合出现在导航页。隐藏容器功能可以把基础组件、临时容器、数据库、辅助服务从导航页移除。

适合隐藏：

- 数据库容器。
- 纯后端 API。
- 临时任务容器。
- 不需要点击访问的基础服务。
- 不希望公开展示的服务。

隐藏后仍可在 Docker 面板中管理容器。

![隐藏容器 / 容器卡片管理](/assets/wiki/hidden-containers.png)

### 集成服务

集成服务用于把外部服务或辅助能力接入面板，方便统一管理。

可用于：

- 添加非 Docker 网站入口。
- 接入常用后台。
- 维护外部工具地址。
- 把 Docker 服务和非 Docker 服务放在同一导航页。

![集成服务 / NAS 接入配置](/assets/wiki/integrated-service-config.png)

![集成服务 / 面板监控效果](/assets/wiki/integrated-service-dashboard.png)

### 搜索引擎

搜索引擎用于配置 Docker-Panel 内置浏览器的网页搜索服务。

在内置浏览器中输入关键词时，系统会使用这里选择的搜索引擎发起网页搜索，方便在不离开面板的情况下查询资料、文档或服务信息。

使用建议：

- 选择自己日常使用的搜索引擎。

### 导航设置

导航设置仅用于配置快捷栏显示位置，以及访问 Docker-Panel 容器时使用的地址模式。

可配置项：

- **快捷栏位置**：设置快捷入口在导航页中的显示位置。
- **面板容器地址选择**：可选择自动、使用内网地址或使用外网地址。

推荐使用**自动模式**。系统检测到当前处于局域网时使用内网地址；离开局域网后使用外网地址。若自动检测不符合当前网络环境，也可以手动切换为内网或外网地址。

### 分组管理

分组管理用于把服务按用途整理。

推荐分组：

- NAS 监控。
- Docker 服务。
- 下载工具。
- 影音媒体。
- 网址收藏。
- PT 站点。
- 管理后台。

分组不建议过多，否则会降低导航效率。

#### 选项卡片作用

- **新增本机容器卡片加入**：选择本机 Docker 自动识别到新容器时，系统默认写入的导航分组。
- **新增 Agent 节点容器卡片加入**：选择所有 Agent 节点自动识别到新容器时，系统默认写入的导航分组。
- **批量分配**：一次为多个已有服务卡片调整分组，适合首次整理或重构导航页。
- **分组列表**：可拖动调整分组优先级，并使用编辑、删除操作维护分类。

若删除当前选中的默认分组，系统会自动切换为分组列表中的第一个分组，不会重新创建默认分组。

![分组管理 / 服务分类](/assets/wiki/group-management.png)

#### 本机与 Agent 默认分组

可分别设置“新增本机容器卡片加入”和“新增 Agent 节点容器卡片加入”的默认分组。本机 Docker 自动识别的新容器与所有 Agent 节点自动识别的新容器，会各自进入预先选择的分组，方便将本地服务与远程服务器服务按用途整理。

![分组管理 / Agent 节点默认分组](/assets/wiki/agent-default-group.png)

## 后端容器面板

后端容器面板对应 Docker-Panel 后台侧边栏中的“容器管理”“Agent 管理”和“配置管理”区域，用于统一管理本机与远程 Docker 节点、容器、Compose、定时重启、镜像、图标库、代理和更新设置。

### Agent 节点管理

Agent 用于把远程 VPS、NAS 或其他 Docker 服务器接入 Docker-Panel。每台服务器作为一个独立节点在线连接，面板集中展示节点数量、在线状态，并可实时查看每个节点的 CPU、内存、磁盘占用以及上传下载流量。

接入后，远程节点的容器会同步到统一工作台与导航页。你可以按节点查看容器状态、资源占用、端口和日志，并执行容器、Compose、镜像、更新等日常管理操作；节点离线时，对应卡片会显示节点离线状态，避免误操作。

节点支持添加、重命名、编辑地址预设、吊销连接和删除。添加节点时使用面板生成的接入信息完成注册，不需要把远程 Docker Socket 暴露到公网。

![Agent 管理 / 多服务器节点总览](/assets/wiki/agent-nodes.png)

### 容器管理

容器管理是集成 Docker 面板的核心。你可以在后台查看所有容器并执行操作。

支持能力：

- 查看容器运行状态。
- 启动容器。
- 停止容器。
- 重启容器。
- 删除容器。
- 查看端口、镜像、运行时间和资源占用。
- 创建更新任务。
- 查看任务进度和日志。

![Docker 工作台 / 容器列表](/assets/wiki/docker-workbench-list.png)

### Compose 项目

Compose 项目页面用于按项目查看和维护服务。

支持能力：

- 查看 Compose 项目列表。
- 查看项目内服务状态。
- 启动、停止、重启项目服务。
- 查看 Compose YAML。
- 可写挂载时保存 YAML。
- 保存后重建项目。

如果只想查看项目，目录可以只读挂载；如果要在线编辑 YAML，需要使用 `:rw`。

![Compose 项目 / 项目列表](/assets/wiki/compose-projects.png)

![Compose 项目 / 项目详情](/assets/wiki/compose-project-detail.png)

### 定时重启

定时重启用于为指定容器配置周期性重启。

可自由设置任务执行周期、每日执行时间、时区，以及多个容器之间的重启间隔和就绪等待时间。周期可按每日、每隔几天或每周配置，并可选择需要执行的容器。

适合：

- 长期运行后资源占用升高的服务。
- 需要定期恢复状态的服务。
- 非关键容器的低峰维护。

不建议对数据库、认证、网关等关键服务随意开启定时重启。

![定时重启 / 任务列表](/assets/wiki/scheduled-restart.png)

### 镜像管理

镜像管理用于处理 Docker 镜像。

支持能力：

- 查看本机镜像。
- 拉取新镜像。
- 查看拉取任务。
- 删除未使用镜像。
- 清理旧镜像。
- 避免删除正在被容器使用的镜像。

### 图标库

图标用于提升导航页识别效率。Docker-Panel 可以根据服务名、镜像名和图标库匹配图标。

图标来源：

- 系统图标库。
- 自动匹配服务图标。
- 内置网址 URL 获取并下载图标。
- 手动上传图标。
- 通过卡片编辑地址自动获取图标。

图标库适合集中维护常用服务图标，例如媒体服务、下载工具、NAS 工具、代理服务、监控服务和自建应用。

![图标库 / 服务图标管理](/assets/wiki/icon-library.png)

### 代理设置

代理设置仅用于 Docker-Panel 检测容器镜像是否有新版本，以及测试 Docker Hub、GHCR、LSCR 的连通性。

填写方式：

- 普通代理：`http://host:port`
- 带认证代理：`http://user:password@host:port`
- 留空：使用直连检测。

真正拉取镜像和升级容器的请求由 Docker daemon 发起。若拉取镜像失败，需要在 Docker daemon 或宿主机的 Docker 服务中单独配置代理。

保存后可点击“测试连通性”确认当前代理是否可用于更新检测。

![代理设置 / Docker 更新检测代理](/assets/wiki/proxy-settings.png)

### Docker 设置

Docker 设置用于控制容器的更新检查、镜像自动升级与旧镜像清理。

#### 更新检查跳过

勾选后的容器不会参与“待更新”扫描，也不会自动进行镜像比较。适合不希望频繁检查或暂不升级的服务。

![Docker 设置 / 更新检查跳过](/assets/wiki/update-check-skip.png)

#### 镜像自动升级

可选择需要自动升级的容器。系统检测到新镜像后会自动创建升级任务，并按任务顺序执行；升级失败时会保留并恢复旧容器，避免服务直接中断。

![Docker 设置 / 镜像自动升级](/assets/wiki/automatic-image-upgrade.png)

#### 旧镜像清理

开启后，容器成功更新并重启完成时会清理本次升级前的旧镜像。若旧镜像仍被其他容器使用，系统会自动保留，不会删除正在使用的镜像。

### 底部与系统操作

对应后台底部的明暗模式、系统设置和退出登录，是日常使用时最常用的基础操作。

#### 明亮模式

明亮模式用于切换界面显示风格。根据使用环境选择明亮或深色显示，方便白天、夜间或大屏展示时查看。

#### 系统设置

系统设置用于进入面板基础配置区域，处理账户、安全、全局偏好和运行参数等设置。

#### 退出登录

退出登录会结束当前后台会话。公共设备或共享电脑使用后建议主动退出。

#### 备份与维护

备份与恢复用于导出和恢复 Docker-Panel 的导航配置与 Agent 节点信息，适合迁移面板、重建环境或在升级前保留当前配置。

导出备份包含：

- 系统设置、导航卡片、分组与集成服务配置。
- Agent 节点信息与凭据校验信息。
- 本地上传的图标、背景等资源。

恢复到**同一面板地址**后，已接入的 Agent 节点可自动重连，不需要逐台服务器重新注册。恢复备份不会覆盖管理员账号和密码。

迁移或升级前建议先导出备份；需要恢复时，在目标面板选择备份文件即可还原导航、节点和相关配置。

![备份与恢复 / 导出与恢复](/assets/wiki/backup-restore.png)

遇到问题时进入 [故障自检](Troubleshooting.md)。
